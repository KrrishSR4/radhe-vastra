const Footer = () => {
  return (
    <footer className="py-8 px-6 bg-primary text-primary-foreground">
      <div className="container mx-auto text-center">
        <p className="text-sm">
          Made with ❤️ — Grey / White Theme — LocalStorage Powered
        </p>
      </div>
    </footer>
  );
};

export default Footer;
